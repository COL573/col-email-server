# Node modules
node_modules/
functions/node_modules/
col-email-server/node_modules/

# Firebase build artifacts
firebase-debug.log
firebase.json
firestore.indexes.json
firestore.rules
storage.rules
functions/lib/
functions/.firebase/

# Render-specific configuration files
render.yaml
render.toml

# Google Drive API configuration files (if any)
google-drive-config.json
google-credentials.json

# SendGrid API key (don't expose)
*.key
*.pem

# Local environment files (prevent secret exposure)
.env
.env.local
.env.production
.env.development
.env.test
.env.*
functions/.env
col-email-server/.env

# 🔐 Supabase credentials (must be stored in ignored .env files only)
# DO NOT commit or expose:
# - SUPABASE_URL
# - SUPABASE_SERVICE_ROLE_KEY
# - SUPABASE_ANON_KEY
# Ensure these are inside .env files only

# Logs and debug files
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# VSCode or IDE settings (optional)
.vscode/

# System files
.DS_Store
Thumbs.db

# OS-specific
*.swp
*.swo

# Optional: exclude build artifacts or zips
*.zip
dist/
build/

# Ignore any other environment-specific or personal files
*.iml
